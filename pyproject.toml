[tool.poetry]
name = "nova_clippy"
version = "0.9b"
description = ""
authors = ["abtsousa <11749310+abtsousa@users.noreply.github.com>"]
readme = "README.md"

packages = [
    { include = "./modules/" },
    { include = "./handlers/" },
    { include = "nova_clippy"}
]

[tool.poetry.scripts]
nova-clippy = "nova_clippy.main:app"

[tool.poetry.dependencies]
# python
python = "^3.11"
# global
typer = {extras = ["all"], version = "^0.9.0"}
inquirerpy = "^0.3.4"

# local
requests = "^2.31.0"
appdirs = "^1.4.4"
bs4 = "^0.0.1"
pandas = "^2.0.3"
tqdm = "^4.66.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
